{
  "name": "gatsby-starter-minimal",
  "version": "1.0.0",
  "private": true,
  "description": "A Gatsby starter with no plugins and a single welcome page.",
  "author": "Laurie Barth",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "develop:stable": "nodemon --delay 750ms --signal SIGINT --watch gatsby-node.js --watch gatsby-config.js --watch src --ext js,jsx,css,json --ignore public --ignore .cache --ignore node_modules --exec \"gatsby develop\"",
    "start": "gatsby develop",
    "prebuild": "node scripts/verify-static-assets.mjs",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "verify-assets": "node scripts/verify-static-assets.mjs"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "license": "0BSD",
  "dependencies": {
    "gatsby": "^5.14.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
