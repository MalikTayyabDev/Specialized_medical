# Gatsby app (see /web)
web/node_modules/
web/.cache/
web/public/
# Duplicate of repo-root static-site (do not commit)
web/static-site/

# OS
.DS_Store
Thumbs.db

# Local env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*

# Editor / local config
.vscode/

# Build artifacts / archives
*.zip

# Netlify static-site build shim outputs (created at build time)
static-site/static-site/
static-site/netlify/
